# ⏰ Digital Clock (JavaScript)

A simple and elegant **digital clock** that displays the current system time in real-time using HTML, CSS, and JavaScript.

The clock updates every second and shows time in **12-hour format with AM/PM**.

---

## 📝 About the Project

This project demonstrates the use of JavaScript’s `Date` object and `setTimeout()` function to fetch and update the current time dynamically.

It is a great project for understanding:
- Real-time updates
- DOM manipulation
- JavaScript timing functions

---

## ✨ Features

- Displays current system time
- 12-hour time format (AM / PM)
- Auto-updates every second
- Clean and minimal UI
- Glowing text effect

---

## 🛠️ Technologies Used

- **HTML5** – Structure
- **CSS3** – Styling and glow effect
- **JavaScript**
  - Date object
  - setTimeout()
  - DOM manipulation

---

## ⚙️ How It Works

1. JavaScript fetches the current time using the `Date` object
2. Hours, minutes, and seconds are formatted properly
3. Time is displayed inside an `<h1>` element
4. `setTimeout()` refreshes the time every second

---

## 📂 Project Structure

```text
Digital-Clock/
│
├── index.html   # HTML structure
├── time.css     # Styling and glow effect
├── time.js      # Clock logic
└── README.md
